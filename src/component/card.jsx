import React from 'react'

const card = ({title , subTitle , description , mrp , price , category , images }) => {  
       
    let singleImage = images?.length > 0 ? images[0] : null 
      
     
  return (  
      
      
<div>gg</div>
  )
}

export default card